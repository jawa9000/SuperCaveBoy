/// add(num1,num2)

var num1 = argument0;
var num2 = argument1
var result = num1 + num2; // local variable that contains the incoming arguments

//show_message(result); // display result in pop up window

return result;
